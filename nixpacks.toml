[phases.setup]
aptPkgs = ["ffmpeg"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "PYTHONPATH=/app/src python -m uvicorn interview_eval.server:app --host 0.0.0.0 --port $PORT"
